# Exact M7 authorization texts

These are separate authorizations. Neither implies the other. Preparation,
commitment, external preregistration, tagging, or corpus construction is not a
substitute for either exact instruction.

## Alpha.3 publication

The exact publication authorization is:

> I authorize publication of all five Lachesis 0.1.0-alpha.3 public packages
> from release commit `e972400a2ff4e65e4b2fe68bc2585f5453e82a06`, using the
> existing audited release workflow, tag `v0.1.0-alpha.3`, npm dist-tag `alpha`,
> and frozen release notes and checksums. I do not authorize M7c execution,
> provider calls, M8 work, or any broader claim.

Any changed commit, version, tag, workflow, note, checksum, package set, or
dist-tag requires a new explicit authorization. This text has not been issued by
the user in this task.

## M7c execution

After external preregistration and complete corpus/environment binding, replace
the two bracketed fields with their immutable values. The exact execution
authorization is then:

> I authorize offline execution of the M7c independent-agent author study under
> preregistration record digest
> `5daee83257122090ac02d4ff99d20baff15517df7345d9fa3368006687624133`, external
> registration identity `[EXTERNAL_REGISTRATION_ID]`, and sealed pre-execution
> binding digest `[CORPUS_AND_ENVIRONMENT_BINDING_SHA256]`. Use only the frozen
> alpha.3 tarballs, documentation, instructions, isolated agent environments,
> adjudication criteria, fixtures, randomization, and analysis code. Stop on
> every preregistered kill gate. I do not authorize package publication,
> human-author enrollment, provider calls, M8 work, or broader claims.

The execution text is invalid while either bracket remains, if any binding
differs, or if external preregistration has not completed. This text has not
been issued by the user in this task.
